# Design — current version

Screenshots of the **live app** (generated by `src/elyx_allocator/render.py`), kept in sync
with the implementation. The earlier exploration mockups (heatmap arc, colour studies,
LeetCode palette, etc.) were pruned once their decisions landed in code.

| File | View |
|------|------|
| `current-week.png` | Week view — sidebar (Upcoming milestones + This-week stats) + central calendar |
| `current-month.png` | Month view |
| `current-event.png` | Event detail drawer (full Activity schema, fields in member-priority order) |
| `current-sidebar-collapsed.png` | Sidebar collapsed via the ☰ toggle — calendar full width |

Regenerate after UI changes:

```bash
PYTHONPATH=src python -c "from elyx_allocator.pipeline import build_site; build_site('web/out')"
# then screenshot web/out/index.html (see the build_site + Playwright snippet in the repo history)
```

Design decisions are recorded in `docs/superpowers/specs/resource-allocator-design.md` (§7 Output).
